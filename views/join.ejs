<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Join The Club</title>
    <link rel="stylesheet" href="/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Kumar+One+Outline&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <% if (!currentUser.ismember) { %>
      <div class="align-s-c">
        <div class="form-container join">
          <h3 class="form-title" aria-hidden="true">
            fenxmi<span class="logo-t">t</span>
          </h3>
          <div class="form-subtitle">Become A Member</div>
          <form action="/join" method="post">
            <label>
              Passcode
              <input
                type="text"
                name="passcode"
                placeholder="Enter Secret Passcode"
                required
              />
            </label>
            <button class="form-btn">Submit</button>
          </form>
          <div class="form-redirect cancel">
            <a href="/" class="a-btn">Cancel</a>
          </div>
        </div>
      </div>
      <div></div>
      <% } else { %>
      <h1>Unauthorised Url</h1>
      <% } %>
    </div>
  </body>
</html>
