<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up</title>
    <link rel="stylesheet" href="/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Kumar+One+Outline&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <div class="align-s-c">
        <div class="form-container sign-up">
          <h2 class="form-title">fenxmi<span class="logo-t">t</span></h2>
          <form action="/sign-up" method="post">
            <label>
              First Name *
              <input
                type="text"
                name="firstname"
                value="<%= locals.values ? values.firstname : '' %>"
                class="<%= locals.error && error.path == 'firstname' ? 'error' : '' %>"
                autocomplete="off"
                required
              />
            </label>
            <label>
              Last Name
              <input
                type="text"
                name="lastname"
                value="<%= locals.values ? values.lastname : '' %>"
                class="<%= locals.error && error.path == 'lastname' ? 'error' : '' %>"
                autocomplete="off"
              />
            </label>
            <label>
              Username
              <input
                type="text"
                name="username"
                value="<%= locals.values ? values.username : '' %>"
                class="<%= locals.error && error.path == 'username' ? 'error' : '' %>"
                autocomplete="off"
                required
              />
            </label>
            <label>
              Password *
              <input
                type="password"
                name="password"
                value="<%= locals.values ? values.password : '' %>"
                class="<%= locals.error && error.path == 'password' ? 'error' : '' %>"
                required
              />
            </label>
            <label>
              Confirm Password *
              <input
                type="password"
                name="confirmPassword"
                value="<%= locals.values ? values.confirmPassword : '' %>"
                class="<%= locals.error && error.path == 'confirmPassword' ? 'error' : '' %>"
                required
              />
            </label>
            <label class="checkbox-label">
              <span>Is Admin?</span>
              <input type="checkbox" name="isAdmin" />
            </label>
            <button class="form-btn">Sign up</button>
          </form>
          <div class="form-redirect">
            Have an account?
            <a href="/" class="a-btn">Login</a>
          </div>
          <% if (locals.error) { %>
          <div class="form-error"><%= error.msg %></div>
          <% } %>
        </div>
      </div>
      <div></div>
    </div>
  </body>
</html>
